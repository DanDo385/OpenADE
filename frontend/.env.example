# Optional frontend API target.
# Leave blank for local dev if you want the Vite proxy to forward /api and /health
# to the local backend on http://localhost:8080.
# Set this to a full backend origin when the frontend should talk to a remote API.
VITE_API_URL=
